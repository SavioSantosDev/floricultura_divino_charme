import { MenuLinksDirective } from './menu-links.directive';

describe('MenuLinksDirective', () => {
  it('should create an instance', () => {
    const directive = new MenuLinksDirective();
    expect(directive).toBeTruthy();
  });
});
