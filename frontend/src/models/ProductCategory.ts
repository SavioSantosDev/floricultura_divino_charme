// Os produtos ofertados serão separados por categoria

export interface ProductCategory {
  image: string;      // Imagem relacionada a esta categoria
  category: string;   // A categoria propriamente dita
}
